const DEV_SERVER_PORT = 5173;
const DEV_SERVER_URL = `http://localhost:${DEV_SERVER_PORT}`;
const WINDOW_ACTION = {
  FOCUS: "focus",
  CLOSE: "close",
  MINIMIZE: "minimize",
  MAXIMIZE: "maximize",
};

export const constants = {
  DEV_SERVER_PORT,
  DEV_SERVER_URL,
  WINDOW_ACTION,
};
